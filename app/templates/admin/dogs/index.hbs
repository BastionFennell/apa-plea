{{#each dog in this}}
  <div class="row">{{dog.id}}</div>
  <div class="row"><img {{bind-attr src=dog.picture}} /></div>
  <div class="row">
    <p>{{dog.description}}</p>
  </div>
  {{#link-to "admin.dogs.edit" dog.id}}<button>Edit</button>{{/link-to}}
{{/each}}
